#include "main.h"

/**
* _strlen - returns the length of string
* @len: lenght of string
* @b: integer to swap
* @temp:
* Return: 0
*/

int _strlen(char *s)
{
int len;
len = _strlen(s);

return (0);
}
