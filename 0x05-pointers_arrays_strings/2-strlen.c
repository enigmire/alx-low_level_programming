#include "main.h"

/**
* _strlen - returns the length of string
* @lenght: lenght of string
* @b: integer to swap
* @temp:
* Return: 0
*/

int _strlen(char *s)
{
int lenght;
lenght = _strlen(s);

return (0);
}
