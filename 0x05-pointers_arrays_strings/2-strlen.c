#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strlen - returns the length of string
* @len: lenght of string
* @b: integer to swap
* @temp:
* Return: 0
*/

int _strlen(char *s)
{
char s[];

return (0);
}
