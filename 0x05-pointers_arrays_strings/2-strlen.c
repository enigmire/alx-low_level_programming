#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strlen - returns the length of string
* @i: initial counter
* Return: 0
*/

int _strlen(char *s)
{
int i = 0;
while (s[i])
i++;
return (0);
}
