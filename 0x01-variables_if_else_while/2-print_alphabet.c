#include <stdio.h>
#include <stdlib.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
int x, z;
for (x = 'a'; x <= 'z'; x++)
{
putchar(x);
}
if (x = z)
{
putchar(' ');
}
return (0);
}
