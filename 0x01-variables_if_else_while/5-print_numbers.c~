#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

/**
* main - Entry point
*
* Retrun: Always 0 (Success)
*/

int main(void)
{
int x;
for (x = 0; x <= 9; x++)
{
printf("%d", x);
}
{
putchar('\n');
}
return (0);
}
