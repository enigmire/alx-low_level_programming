#include "main.h"

/**
* _isalpha - check for alphacase letters
* @r : character to check the case
* Return: 0 or 1
*/

int print_sign(int n)

{
return ((n > 0) && printf(+));
return ((n = 0) && printf(0));
return ((n > -1) && printf(-));
}
